<!DOCTYPE html>
<html lang="zh">
  <head>
    <meta charset="UTF-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no"/>
    <title>{{ title or "VOIDHEAD" }}</title>
    <link rel="stylesheet" href="{{' ' | url }}">

  </head>
  <body>
    {{ content | safe }}
    </body>