<!DOCTYPE html>
<html lang="zh">
  <head>
    <meta charset="UTF-8"/>
    <title>{{ title or "VOIDHEAD" }}</title>
    <link rel="stylesheet" href="{{'/assets/style.css' | url }}">

  </head>
  <body>
    <div class="wrapper">
      <div class="figure">
        <img src="{{'/assets/main/figure.webp' | url }}" alt="mascot"/>
      </div>
      <div class="bulletin-frame">
        <img src="{{'/assets/main/bulletin-border.png' | url }}" class="bulletin-border"/>
        <div class="bulletin-content">
          <h2>公告栏</h2>
        </br>
        <p>
          To-do list</br>
      </p>
      <ul>
        <li>Make the blog section neat</li>
        <li>Write abt serverless guestbook and some micro bugs</li>
        <li>Read log in blog index, read.md</li>
        <li>About(hobby actually?) and <del>free material page</del></li>
        <li>Draw more stuffs for the site... write ideas on pratibimba</li>
      </ul>

    </div>
  </div>
  <header>
    <img src="{{'/assets/main/header.webp' | url }}" alt="Header Image" class="header-image"/>
    <a href="/guest" id="guest">Sign my Guestbook!</a>
  </header>

  <main>
    {{ content | safe }}
  </main>

  <footer>
    <small>placeholder for counter?</small>
  </footer>

  <img src="{{ '/assets/main/ratseal.png' | url }}" alt="rat seal" id="ratseal"/>
  <img src="{{ '/assets/main/xushou.png' | url }}" alt="xushou" id="xushou"/>
  <img src="{{'/assets/main/kongnao.png' | url }}" alt="kongnao" id="kongnao"/>
</div>
</body>

<script>
  document.querySelectorAll(".copy-btn").forEach(btn => {
    btn.addEventListener("click", () => {
      const pre = btn.nextElementSibling;
      const text = pre.innerText;

      navigator.clipboard.writeText(text).then(() => {
        btn.textContent = "Copied!";
        setTimeout(() => (btn.textContent = "Copy"), 1500);
      }).catch(() => {
        btn.textContent = "❌ Failed";
      });
    });
  });
</script>

</html>